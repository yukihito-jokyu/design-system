# filtering

## 適用条件

一覧に検索語以外の条件選択がある場合。条件がなければ適用しない。

## 操作と状態

`table-toolbar`に条件を置き、`data-table`へ`filterRow`と条件を表す`filterKey`を渡す。適用・解除後の件数を表示し、`filterKey`変更時にページと選択を初期化する。条件に一致しない0件は初回0件と区別し、条件解除を次の行動にする。

## 狭い画面とアクセシビリティ

狭い画面では条件と件数を一覧より前に縦に配置する。条件はラベル付きの操作にし、件数と空状態を読み上げる。条件変更でフォーカスを奪わない。
