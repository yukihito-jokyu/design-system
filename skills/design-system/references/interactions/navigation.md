# navigation

## 適用条件

階層移動、戻り先、モバイルメニューが必要な場合。単独画面に不要なら適用しない。

## 操作と状態

外側の画面枠は`app-shell`、階層は`ui/breadcrumb`、詳細の戻り先は`detail-page`の`back`で示す。現在地と戻り先を区別する。URLと遷移は利用側で決める。

## 狭い画面とアクセシビリティ

狭い画面では`app-shell`のモバイルメニューを使う。メニューの開閉とリンクはキーボードで操作でき、閉じたら起点へフォーカスを戻す。
