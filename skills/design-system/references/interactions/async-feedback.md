# async-feedback

## 適用条件

通信や保存が非同期の場合。同期的な画面には適用しない。

## 操作と状態

初回待機には`loading-state`、失敗と再試行には`error-state`、短い成功通知には`ui/toast`を使う。結果を画面に残す要件なら`result-page`を選ぶ。更新中は二重送信を防ぎ、失敗後は入力値を保持する。

## 狭い画面とアクセシビリティ

待機・成功・失敗を読み上げ、再試行へキーボードで到達できる。読み込み完了だけでフォーカスを奪わない。
