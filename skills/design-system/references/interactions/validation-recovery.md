# validation-recovery

## 適用条件

フォームの入力検証で修正が必要な場合。通信失敗だけなら`async-feedback`。

## 操作と状態

`form-field`の`error`で項目に`aria-invalid`と説明を結び付け、`ui/alert`にエラー概要を示す。最初の無効項目へフォーカスし、入力値を保持して再送信できる。検証規則は利用側が渡す。

## 狭い画面とアクセシビリティ

狭い画面でも概要から項目へ移動できるようにする。エラーを色だけで示さず、文面を読み上げる。
