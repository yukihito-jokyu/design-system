# OrbitSavedStatusIcon

- Registry: `@yukihi/orbit-saved-status-icon`
- Source: `registry/new-york/icons/orbit-saved-status-icon/OrbitSavedStatusIcon.tsx`

## 導入

```sh
npx shadcn@latest add @yukihi/orbit-saved-status-icon
```

## 使用例

```tsx
import { OrbitSavedStatusIcon } from "@/components/icons/OrbitSavedStatusIcon";

<OrbitSavedStatusIcon label="OrbitSavedStatusIcon" />;
```

必要な状態・Propsは導入後の型定義で確認し、Tokenとアクセシビリティ属性を維持する。
